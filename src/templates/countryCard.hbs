
<h1 class="card-title">{{name}}</h1>
<div class='card'>
    <p class="card-capital"><span class="card-text">Capital:</span>{{capital}}</p>
    <p class="card-population"><span class="card-text">Population:</span>{{population}}</p>
    <ul class="card-list">
        <span class="card-text">Languages:</span>
        {{#each languages}}
        <li class='card-item'>{{this.name}}</li>
        {{/each}}
      </ul>
     <img src='{{flag}}' alt='{{name}}' class="card-flag">
</div>
